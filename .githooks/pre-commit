#!/bin/sh
# Pre-commit hook: fast lint checks (runs before every commit).
set -e
export UV_CACHE_DIR="${TMPDIR:-/tmp}/uv-cache"
cd "$(git rev-parse --show-toplevel)/TRACE" 2>/dev/null || cd "$(git rev-parse --show-toplevel)"
make vet lint
